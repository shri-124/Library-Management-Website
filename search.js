function submitSearch() {
    var searchInput = document.getElementById('search-input');
    alert('Search query: ' + searchInput.value);
    // Additional actions can be performed with searchInput.value here
}